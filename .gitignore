# agentic harness (installed)
__pycache__/
*.pyc
.pytest_cache/
.env
.env.*
.mcp.json
.claude/state/*
!.claude/state/.gitkeep

# stray scratch/verification output (never a project artifact)
*.err
gen*.json
/scratch/

# M6a panel research contract: raw wayback fetch cache (resumable; huge; not a
# project artifact — data/panel/wayback_observations.jsonl and panel.jsonl ARE
# committed artifacts and stay tracked).
data/panel/raw_cache/
